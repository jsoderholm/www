---
import ThemeToggle from "@/components/theme-toggle.astro"
import SearchButton from "@/components/search-button.astro"

interface Props {
  title: string
}

const { title } = Astro.props
---

<header class="mb-7 flex items-center justify-between">
  <h1 class="text-accent-foreground font-bold">{`${title} ﹒ jsoderholm`}</h1>
  <div class="flex gap-1">
    <SearchButton />
    <ThemeToggle />
  </div>
</header>
